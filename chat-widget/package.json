{
  "name": "chat-widget",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --format esm,iife --dts --minify --clean --sourcemap",
    "postbuild": "cp dist/index.global.js test/index.global.js",
    "dev": "tsup src/index.ts --format esm --watch --sourcemap",
    "lint": "eslint \"src/**/*.ts\"",
    "prepare": "rimraf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.12.7",
    "eslint": "^8.57.0",
    "rimraf": "^5.0.5",
    "tsup": "^7.2.0",
    "typescript": "^5.4.0"
  }
}

